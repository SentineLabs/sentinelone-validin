$validin = $lib.import(s1.validin)


function main(input) {
    yield $validin.certificates($input, $cmdopts)

    /* bytes not available for ctstream certificates yet
    if $cmdopts.download {
        | s1.validin.download
    }
    */
}


divert $cmdopts.yield $main($node)
