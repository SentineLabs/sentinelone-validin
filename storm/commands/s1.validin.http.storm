$validin = $lib.import(s1.validin)


function main(input) {
    yield $validin.http($input, $cmdopts)

    if $cmdopts.download {
        | s1.validin.download
    }
}


divert $cmdopts.yield $main($node)
