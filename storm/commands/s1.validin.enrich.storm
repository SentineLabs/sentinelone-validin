$validin = $lib.import(s1.validin)


function main(input) {
    yield $validin.enrich($input, $cmdopts)

    if $cmdopts.download {
        +(crypto:x509:cert or inet:http:request) | s1.validin.download
    }
}


divert $cmdopts.yield $main($node)
